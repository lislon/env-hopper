# Default environment variables for backend-example
# These are used if not overridden by .env or .env.local

# Database - PostgreSQL
EH_CORE_DATABASE_URL="postgres://env-hopper:env-hopper@localhost:5532/env-hopper-core"

# Server
NODE_ENV=development
PORT=4001
# hopper | catalog
VITE_EH_MODE=hopper
