// The kit type-checks the app's sources, which import svg assets as React
// components. At runtime the same `vite-plugin-svgr` the app builds with
// resolves them, so nothing needs mocking — see this package's vite config.
/// <reference types="vite-plugin-svgr/client" />
