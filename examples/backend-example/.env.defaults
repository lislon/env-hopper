# Default environment variables for backend-example
# These are used if not overridden by .env or .env.local

# Database - PostgreSQL
EH_CORE_DATABASE_URL="postgres://user:pass@localhost:5532/env-hopper-core"

# Server
NODE_ENV=development
PORT=4001
